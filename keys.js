// keys (property)
const computer = {
    brand : 'hp',
    price :40000,
    processor : "inter",
    ssd : 128
}
const keys = Object.keys(computer)
console.log(keys)

// values 
const values = Object.values(computer)
console.log(values)